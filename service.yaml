apiVersion: v1
kind: Service
metadata:
  name: vault-mcp-server
  namespace: kagent
  labels:
    app: vault-mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8084
    targetPort: 8084
    protocol: TCP
    name: http
  selector:
    app: vault-mcp-server
